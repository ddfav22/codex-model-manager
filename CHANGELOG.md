# 更新日志

本项目遵循 `主版本.次版本.修订版本` 的递增方式。这里只记录适合公开发布的信息；本机路径、测试凭据和内部部署记录不会进入仓库。

## 1.2.86 - 2026-08-07

### 修复 1.2.85 自动发送前的 TypeError

- 修复自动续接调用了未公开导出的 app-server 请求函数，导致运行时在真正发送前抛出 `TypeError` 的问题。请求函数现在是正式 manager API，并由实际 runtime 装配测试覆盖。
- 自动续接不再复用人工任务恢复的 session 扫描、工作区和 Git 快照检查。终止诊断已经携带 thread/turn ID，客户端现在只解析现有 `codex.exe`，以 Codex Home 作为安全启动目录，然后立即执行 `turn/steer` 或 `turn/start`。
- 优先复用本次渠道应用时已经验证的 Codex 运行时路径，缺失时再进行本机发现。普通对话历史缺失、session 元数据异常或工作区不可访问不再阻止发送可见的“继续”；人工恢复入口的严格检查保持不变。
- 自动续接失败日志新增 `resolve-target`、`turn-steer`、`turn-start` 或 `exec-resume` 阶段，以及有界且经过凭据特征清理的错误消息，避免现场只记录一个无法定位的 `TypeError`。

### 测试

- 新增真实 main runtime 接线、app-server proxy JSON-RPC 写入序列、无 session ID 即可解析 continuation target、显式运行时路径优先、steer→start 状态切换、socket resume fallback、目标解析失败阶段、权限失败阶段和错误消息记录回归。

## 1.2.85 - 2026-08-07

### 终止后立即发送“继续”

- 删除 1.2.84 自动续接中的停滞观察器、后续进展取消和最长五分钟空闲轮询。检测到“无法显示此内容”对应的异常终止、受阻工具调用或真实传输中断后，客户端立即向同一任务发送一条可见的“继续”。
- 活动回合使用 Codex 官方 `turn/steer` 注入消息；回合已经结束时立即使用 `turn/start`，若状态在发送瞬间变化则在两者之间切换一次。桌面控制连接不可用时仍保留官方 `codex exec resume` 兜底，不重放原始 HTTP 请求。
- 同一连续任务仍最多自动发送三次；相同回合的重复诊断合并，第四次熔断。权限、认证和不可恢复错误不会被当作连接故障绕过。

### 测试

- 新增无观察延迟、无状态轮询、活动回合直接 steer、已结束回合直接 start、发送瞬间状态切换、三次上限、socket 兜底和权限失败回归。
- 恢复检查新增显式跳过运行时状态读取的测试，确保自动续接只解析本地任务位置，不会在发送前等待任务空闲。

## 1.2.84 - 2026-08-07

### 任务停滞与连接中断后继续

- 任务监督器现在会观察原生 Responses 的终态工具调用和真实传输中断；如果 Codex 随后没有产生新请求且任务已经空闲，就在同一对话中发送一条可见的“继续”。任何后续进展都会取消待发消息，避免与 Codex 自己的重试或工具回环抢跑。
- 客户端主动取消、响应关闭和真实网络错误现在分开记录。只有网络错误或上游超时可能进入停滞恢复，内部错误和用户停止不会被自动覆盖；代理仍不会重放原始 HTTP 请求。
- refusal、空输出、受阻工具调用和传输中断共用同一连续计数，同一任务最多自动发送三次“继续”，第四次熔断。

### 原生 Responses 图片显示

- NewAPI 直接在 Responses 流中返回 `image_generation_call.result` Base64 时，客户端现在会校验 PNG/JPEG/WebP 内容并保存到 `data/generated-images`，再在同一响应结束前追加标准 assistant 图片 Markdown。Codex 不再只是把数 MB Base64 留在会话历史而不显示图片，也不再依赖模型主动复述工具提示。
- 原始图片调用仍原样保留；诊断只记录发现、物化、失败和注入数量，不记录 Base64、提示词或本地文件名。无效、超限或不支持的图片不会中断原始模型响应。

### 界面与 Windows 图标

- 为任务进行中的圆形进度图标补充稳定的旋转动画，避免样式注入顺序变化后静止。
- 移除主界面、托盘、预加载桥接和 IPC 中面向用户的“打开运行日志”入口；脱敏运行日志仍在后台保留，供维护诊断使用。
- 窗口和托盘统一使用同一个多尺寸 Windows 图标，并将 ICO 重建为兼容 Windows 原生解码器的 DIB 图像，修复小尺寸图标花屏、透明度异常或显示失败。

### 测试

- 新增受阻工具调用、真实网络中断、客户端取消、后续进展取消续接、空闲等待中止和最多三次限制的回归测试。
- 新增原生流式 Base64 图片物化、assistant Markdown 注入、无效图片安全降级和 wire 端到端回归。
- 打包界面测试新增旋转动画与日志入口移除断言；图标测试新增多尺寸 DIB 格式、透明像素比例和 Windows 原生解码检查。

## 1.2.83 - 2026-08-07

### 任务终止后在同一对话继续最多三次

- 删除 1.2.82 在协议代理内部重发同一 Responses 请求的实现。代理现在完整观察终态并原样交付响应，不再追加隐藏提示或替用户重复上游请求。
- 当原生 Responses 以 refusal、空输出、仅推理无最终答复或 incomplete 终止时，客户端等待当前 Codex turn 真正结束，再通过官方 `turn/start` 向同一 thread 新建一条可见的“继续”消息。
- 同一连续终止链最多自动继续三次；第四次终止只记录熔断，不再创建 turn。正常最终答复、用户手动发送新消息、需要用户输入或认证/额度/权限/网络错误会停止或重置计数。
- 优先连接桌面端现有 app-server，使新消息即时进入当前对话；桌面控制 socket 不可用时，使用官方 `codex exec resume` 兜底。两条路径都继承原任务审批与权限，不添加绕过参数。
- 运行日志只记录脱敏任务引用、尝试次数、终止分类和启动路径，不保存“继续”以外的对话内容、refusal 正文、工具输出或凭据。

### 测试

- 新增三次上限、第四次熔断、重复终止去重、正常完成清零、用户新 turn 重置、不可恢复错误停止、并发诊断合并、桌面 app-server 与 resume fallback 回归。
- 更新 wire 端到端测试，覆盖正常文本、流式空终态、refusal、仅推理终态和非流式 JSON 空终态，并断言每次模型响应只产生一个上游请求。

## 1.2.82 - 2026-08-07

### 修复 Responses 空终态显示“无法显示此内容”

- 原生 Responses 渠道明确返回 `response.completed`、但没有任何可渲染文本、推理摘要或工具调用时，客户端会保留原任务和工具结果并自动续接一次，避免 Codex 直接显示“无法显示此内容”。
- 正常响应在探测到第一个真实输出后立即透传，不等待完整响应；工具调用和可见推理同样视为有效输出，不会误触发续接。
- 一次续接后仍为空时，返回可见中文停止提示并保留手动恢复入口，不建立循环。续接遇到认证、额度、权限、网络或上游错误时立即停止并返回原错误。
- 诊断只记录是否尝试、是否恢复、是否连续为空、探测字节数和上游状态，不记录模型正文、恢复提示、工具输出或凭据。

### 测试

- 新增正常原生 Responses 不续接、流式空一次恢复、非流式空一次恢复、连续空两次停止、空后认证失败不再尝试及诊断脱敏分类回归。

## 1.2.81 - 2026-08-06

### 手动恢复未完成的 Codex 任务

- 对话管理新增“恢复未完成任务”，支持直接粘贴任务 UUID 或从对话行进入。用户确认后，客户端先只读检查任务与最后一轮状态、工作目录和 Git 已跟踪改动，再通过 Codex 官方 `exec resume` 接着处理未完成工作。
- 恢复提示从 stdin 传递，不出现在进程参数或日志；日志只保留脱敏任务引用、动作、阶段和失败分类，不记录对话、工具输出、文件名或凭据。
- 只有原任务在新 turn 尚未开始前因会话记录损坏或协议不兼容而失败，才创建一次 Fork 兜底。认证、额度、高负载、权限、审批、网络和未知错误均停止，不会在后台循环发送“继续”。
- 活动任务和最后一轮仍为 `inProgress` 的任务会被拒绝恢复；恢复进程随客户端退出而终止，避免两个执行器同时修改同一工作区或留下无人监督的进程。

### 测试

- 新增任务 ID 校验、stdin 指令、活动回合拒绝、原任务成功恢复、会话损坏 Fork、认证/高负载/权限/网络停止、重复恢复拒绝、Git 现场摘要和敏感信息隔离回归。

## 1.2.80 - 2026-08-06

### Codex 对话显示生成图片

- 修复 NewAPI 已成功生成图片、但上游返回 URL 时 Codex 对话只看到工具完成而不显示图片的问题。图片 MCP 现在默认请求 `b64_json`；URL 响应会立即下载并转换为标准 MCP `image` 内容块。
- 生成图片同时保存到客户端 `data/generated-images`，工具结果要求模型在最终回答中原样嵌入本地绝对路径 Markdown；即使 Codex 不直接展示工具图片块，最终对话仍可渲染图片。
- URL 下载不携带 NewAPI Key，只接受 HTTPS 和 PNG/JPEG/WebP，拒绝 localhost、私网地址、危险重定向、伪图片及超过 20 MiB 的内容。下载失败时保留原 URL Markdown 兜底，不会再次调用图片生成接口或重复计费。

### 测试

- 新增 URL 图片物化、本地文件、最终回答 Markdown、私网拒绝、下载失败降级、MCP 默认 `b64_json` 和 wire 端到端回归。

## 1.2.79 - 2026-08-06

### 轻量补丁更新

- 客户端从本版本开始识别与当前版本、Electron 运行时完全匹配的 `app.asar` 轻量补丁；后续兼容版本通常只需下载约 2–3 MB，不再每次下载约 80 MB 的完整 NSIS 安装包。
- 补丁名称、GitHub Release 地址、容量和 SHA-256 均需通过校验；缺少补丁、运行时不兼容、下载损坏或此前启动回滚时会自动改用完整安装包。
- 补丁安装只允许替换当前程序目录下的 `resources/app.asar`。新版本窗口启动后必须回写健康标记，否则辅助进程会恢复旧 `app.asar` 并重启旧版。
- 1.2.78 的旧更新器尚不认识补丁，因此升级到 1.2.79 仍需最后下载一次完整安装包；轻量更新从 1.2.79 升级后续版本时开始生效。

### 测试

- 新增补丁优先选择、损坏回退完整包、路径约束、双重摘要校验、启动健康确认、失败回滚、运行时兼容发布门禁和补丁产物测试。

## 1.2.78 - 2026-08-06

### Codex 任务诊断

- 从 Codex 官方请求元数据中只提取规范的任务、回合与会话 UUID，使 NewAPI 请求失败可以精确对应原任务；对话正文、额外元数据、工具参数和凭据不会进入诊断。
- 高负载、限流、超时、上下文过大、代理连接中断、Agent Loop 连续传输失败和重复中间计划现在使用不同的诊断类别与日志等级。
- 管理器会显示本次运行中最新的警告或错误摘要，并可直接定位完整运行日志；摘要只包含分类后的中文说明、模型、时间和任务 UUID。

### 行为边界

- 本版本不新增异常授权窗口，不自动发送“继续”，也不在任务结束后发起新的自动重试；提示仅用于观察和排障。

### 测试

- 新增任务元数据白名单、敏感字段排除、容量/超时分类、真实连接中断、Agent Loop 停滞分类和公开摘要脱敏回归测试。

## 1.2.77 - 2026-08-06

### NewAPI 图片 Token 路由

- 图片生成默认模型更新为 `grok-imagine-image-quality`，与当前 Grok Imagine 图片接口保持一致；工具调用仍可显式指定其他兼容图片模型。
- NewAPI 同步后会从同一账号的全部可用 Token 中独立选择 `/v1/models` 实际返回图片模型的 Key。聊天继续使用用户当前选中的 Token，不再要求为了生成图片而切换聊天渠道。
- 图片模型无权访问时显示明确中文提示，引导用户在 NewAPI 控制台授权图片模型并重新同步；上游请求 ID、Key 和提示词不会进入错误信息或日志。

### 测试

- 新增图片模型识别与优先级、聊天/图片 Token 隔离、官方默认模型、图片权限拒绝脱敏等回归测试。

## 1.2.76 - 2026-08-06

### 普通对话与项目边界

- 修复普通 Codex 对话关闭后被管理器按 cwd 自动转换为 Local Project 的问题。`projectless-thread-ids` 现在始终优先，管理器只同步已存在的显式项目和任务归属，不再仅凭对话工作目录创建或固定项目。

### NewAPI 图片生成

- 本地协议代理新增 OpenAI 兼容的 `POST /v1/images/generations` 路由，并把请求转发到当前选中的 NewAPI 渠道；API Key 只保留在管理器进程内。
- 管理器为 Codex 注册受随机本地能力路径保护的 `generate_image` MCP 工具。Grok 原生工具不兼容时会优先调用这个可审计工具，不再依赖运行时是否提供 OpenAI 内置图片工具。
- 同时支持上游 `url` 与 `b64_json` 响应；内联图片只接受 PNG、JPEG、WebP，单图上限 20 MiB。提示词、API Key 和完整图片数据不进入诊断日志。
- 图片生成默认使用 `grok-imagine-image`，工具调用可以显式覆盖渠道实际使用的图片模型；单次最多 4 张，同一渠道只允许一个生成任务，避免意外并发计费。

### NewAPI 聊天与工具调用

- 参考 Nous Research 的开源 Hermes Agent 协议自愈思路，新增独立 NewAPI Chat Completions 兼容层；Codex 仍拥有工具执行与 Agent Loop，不引入第三方运行时或远程工具网关。
- 发送前在临时请求副本上清理严格兼容接口会拒绝的空工具数组、孤立工具结果、不完整调用、缺失/重复 call ID 和非字符串参数；不会修改持久化对话，也不会伪造工具已成功执行。
- 仅当上游以 HTTP 400/422 明确指出可选字段不兼容时，最多执行三次定向重试，依次移除被拒绝的 `stream_options`、`parallel_tool_calls`、`tool_choice`、推理/服务字段或函数 `strict`；鉴权、限额、容量、上下文和普通业务错误不会触发该降级。
- 流式工具调用支持延迟 call ID、重复 call ID、拆分或重复工具名称、对象参数以及兼容端点把增量放在 `message` 而非 `delta` 的情况；输出保持标准 Responses 工具事件和稳定配对。

### 发布说明

- 本版本已完成 `.230` 现场验证并发布正式 GitHub Release。

## 1.2.75 - 2026-08-05

### 映射盘启动

- 管理器在 Electron 初始化前禁用硬件加速，避免便携版从映射盘或网络盘启动时 GPU 子进程反复失败并导致主进程退出。
- 启动时检测 Windows 驱动器类型：本地磁盘继续使用 Chromium 沙箱；只有确认位于网络盘时才启用 Electron 映射盘兼容开关，检测失败也保留沙箱。
- 管理器界面不依赖 WebGL；软件渲染只影响该轻量管理窗口，不改变 Codex、模型代理、本地工具或用户数据。
- `process.start` 日志新增硬件加速、Chromium 沙箱兼容状态和脱敏的驱动器类型，便于确认启动模式已经生效。

## 1.2.74 - 2026-08-05

### Grok 容量与上下文错误

- 修复 `currently experiencing high demand` 被误判成“不支持原生工具”的问题；容量繁忙不再错误触发提示词工具模拟。
- 上游明确返回容量繁忙时最多执行两次有限退避重试，并遵守受限的 `Retry-After`；普通限额、鉴权错误和上下文过大不会盲目重试。
- 容量繁忙重试耗尽与上下文超过模型限制会显示不同的中文提示，便于判断应当稍后继续、切换渠道，还是压缩上下文或新建任务。

### 日志与诊断

- 主界面新增“打开运行日志”，不再只能通过托盘定位 `manager.log`。
- 上游失败以 `proxy.upstreamError` 警告记录状态码、脱敏失败类别、重试次数、延迟和请求字节数；不记录对话正文、工具参数、API Key 或上游错误正文。
- 新增容量恢复、容量耗尽、上下文过大、错误分类、主界面入口与预加载桥接回归测试。

## 1.2.73 - 2026-08-05

### Agent Loop

- 兼容上游偶发返回的 `0xa0a1e…0xa1…0xa2…` 编码工具帧，将允许的调用还原为标准 Codex 工具事件，不再把内部编码当作普通文字显示后结束对话。
- 编码帧与字段标记支持跨 SSE 分片暂存；只采用当前请求允许的第一个工具调用，后续执行和等待仍由 Codex 工具结果驱动，避免执行模型预写的陈旧步骤。

### 测试

- 新增 `exec`/`wait` 编码字段、数值和布尔参数、无字段与非法工具名回归，并覆盖完整编码工具帧被拆成四个 SSE 分片后的 wire 转换与不可见性。

## 1.2.72 - 2026-08-05

### 对话显示

- 新增统一的可见文本净化模块，过滤上游二次转义后只剩 `\n` 的空白回复，并把连续三行以上的空白压缩为正常段落间距。
- Grok 返回 HTML/JavaScript 工具外壳或原始 `<codex_tool_call>` 协议时，只向 Codex 发送标准工具事件，不再把 HTML、脚本、内部协议或空白进度卡显示在对话框中。
- 净化同时覆盖 Agent Loop、直接 Chat→Responses 流式转换和非流式转换；Markdown 代码块中的反斜杠内容与普通 HTML 回答保持不变。

### 测试

- 新增字面量转义换行、JSON 二次编码空白、HTML 工具外壳、原始工具协议、代码块保真，以及直接流式旁路的模块和 wire 回归测试。

## 1.2.71 - 2026-08-05

### Agent Loop

- Grok 长上下文恢复请求的单次等待由 15 秒延长至 60 秒，避免把仍在正常推理的请求误计为连续连接失败。
- 恢复失败会脱敏区分等待超时、限流、上游服务错误、请求拒绝和传输错误，并显示对应的中文操作提示。

### 测试

- 新增超过旧 15 秒边界后仍产生标准 Codex 工具调用的 wire 回归，以及 HTTP/超时失败分类与中文提示测试。

## 1.2.70 - 2026-08-05

### Agent Loop

- 工具结果后的回答只要缺少明确完成信号，就不再因一次 15 秒续接确认超时而推断任务完成；正常恢复继续保持无固定轮数，只由工具调用、明确完成、需要输入、用户取消或安全熔断收口。
- 识别 Codex 的手动中断控制消息；用户随后说“继续/继续安装并验证”等内容时，自动锚定原始任务、最近可见进度和已保留工具结果，从中断点继续。
- 手动中断控制消息会在发往上游前清除，不作为普通对话、系统说明或模型可复述内容暴露。

### 测试

- 新增未签名终局连续恢复、五次相同回答熔断，以及安装 Python 过程中手动中断后继续的 Agent Loop/wire 回归。

## 1.2.69 - 2026-08-05

### Agent Loop

- 完成和安全停止标志现在参与流式控制边界检测；即使标志被拆成多个上游分片，也不会先作为进度文字显示在 Codex 对话中。
- 扩展“接下来用/通过脚本排查、认证、验证”等方法切换计划识别，工具结果后的这类承诺会继续进入无固定轮数恢复，不再因一次恢复请求超时被误判成最终回答。

### 测试

- 新增分片完成标志与脚本认证计划的 Agent Loop/wire 回归，验证对话只收到正常进度和标准工具事件，内部控制标志与原始模拟协议不可见。

## 1.2.68 - 2026-08-04

### Agent Loop

- Grok 计划续接取消固定三次和 40 秒总上限，只要任务仍未完成就继续请求下一条可执行工具决策；完成、确实需要用户输入、有效工具调用或用户取消会立即结束当前恢复。
- 保留单次请求超时，并新增连续五次渠道失败、连续五次完全相同计划的死循环熔断；熔断使用普通中文提示，不显示内部 Agent Loop 控制标记。

### 测试

- wire 回归让上游连续四次只返回不同计划，第五次才产生工具调用，证明第三次之后仍会继续；同时覆盖连续连接失败和相同内容复读的失败路径。

## 1.2.67 - 2026-08-04

### 修复

- Grok 在工具结果后回复“改用另一种方式继续”时会被识别为未完成步骤，并立即进入有上限的工具续接，不再把方法切换计划误当成最终结果。
- 渠道不支持 `response_format` 时，会在同一次续接机会内自动降级为普通流式 JSON 请求，避免兼容性探测白白消耗最多三次的防死循环额度。
- 流式工具标签即使被拆成 `<codex_tool_cal` 与 `l>` 等多个分片，或只返回到标签名，也会在第一个前缀字节处截断，不再把 `<codex_tool_call` 和内部参数显示在 Codex 对话框。

### 测试

- 新增用户 SSH/Posh-SSH 场景回归、分片工具标签 wire 回归和结构化输出降级回归；验证下一工具调用仍以标准 Responses `custom_tool_call` 事件交给 Codex。

## 1.2.66 - 2026-08-04

### 构建兼容性

- Windows 打包器从 electron-builder 25.1.8 固定升级到 26.15.3，使用新版 NSIS/Windows 兼容逻辑处理纯净云端 Windows 上安装器在解压前偶发的 `0xC0000005`。
- 应用运行时、渠道逻辑和数据格式保持不变；生产依赖审计仍为 0 项漏洞，开发工具链的既有大版本安全升级另行验证，不混入本次安装器修复。

## 1.2.65 - 2026-08-04

### 测试与诊断

- 安装器执行失败时记录 Windows Application Error 事件和安装目录残留状态，便于区分 NSIS、系统模块、文件占用和安装后退出码异常；诊断不包含用户凭据。
- main CI 也必须构建 NSIS 并通过完整安装、卸载、重装、原位更新和自动重启回归，不再等到正式版本标签阶段才执行安装器测试。

## 1.2.64 - 2026-08-04

### 修复

- 安装回归测试现在会等待 NSIS 卸载器及其临时子进程完成文件清理，再执行重装或删除测试目录，避免较慢的纯净 Windows 环境出现卸载与重装竞态。

### 测试

- 保留完整的首次安装、卸载保留数据、原路径重装、应用内更新、自动重启、再次卸载和默认安装目录验证；任何一步失败都不会发布 GitHub Release。

## 1.2.63 - 2026-08-04

### 新增与优化

- Windows 安装包改为安装向导，用户可以自行选择安装目录；静默在线更新和当前目录数据保留逻辑不变。
- 应用程序、安装器、卸载程序和网页图标统一更换为新的模型路由图标，并提供 Windows 多尺寸 ICO。

### 修复

- Grok 流式计划消息后若复述旧版 `[Codex local tool calls]`，代理会在内部标记出现的第一个字节前停止可见增量，避免原始命令、参数 JSON 和本机路径短暂闪现在 Codex 对话框。
- 旧版多行工具 JSON 改用支持嵌套对象、数组、引号和转义字符的边界扫描清理；标准 Responses 工具事件和 Agent Loop 执行链路不受影响。

### 测试

- 新增安装向导目录选择配置和全部图标资源的模块约束。
- 新增多行嵌套工具 JSON 单元回归，以及“计划增量 + 后续内部工具分片”的 wire 回归，断言原始 PowerShell、路径、内部标记和重复工具事件均不会下发。

## 1.2.62 - 2026-08-04

### 修复

- Grok 在工具结果后只回复“下一步继续”时，有限恢复不再依赖模型自行附加内部结束标志，而是要求明确选择“继续调用工具、任务确实完成、确实需要用户输入”三种结构化决策。
- 结构化决策只在代理内部解析；最终答复或具体问题仍以正常 Codex 消息显示，决策 JSON、内部完成信号和工具历史不会进入对话正文。
- 严格恢复的输出预算从 512 token 调整为 4096 token，避免多步骤任务中的 `exec` 工具脚本因截断而无法执行；最多三次、单次 15 秒和总计 40 秒的防死循环限制保持不变。

### 测试

- 新增三态决策解析、空结果拒绝、显式等待用户输入和恢复 token 预算单元回归。
- wire 回归覆盖结构化 `tool` 与 `complete` 决策，验证它们分别转换为标准 Codex 工具事件和纯净最终答复，并记录不含正文的决策类型诊断。

## 1.2.61 - 2026-08-04

### 修复

- Grok prompt-emulated 工具历史不再使用可能被模型复述的 `[Codex local tool calls]` / result 文本，改为仅供代理上下文使用的结构化内部信封。
- 可见结果会剥离新旧内部工具历史和适配提示；即使上游复述旧工具 JSON，也会先清洗再进行工具解析，避免把历史记录误判成新的工具调用和重复执行。
- 工具调用、工具结果和 `call_id` 关联仍通过标准 Responses 事件交给 Codex，Agent Loop 能力不受影响。

### 测试

- 新增内部信封生成、工具结果识别、新旧标签清洗和上下文分类单元回归。
- 新增协议线回归：模拟上游复述旧标签及 `exec` JSON，断言下游只出现正常答案、不出现内部文本且不生成重复工具事件。

## 1.2.60 - 2026-08-04

### 优化

- 参考 Hermes Agent 的流式回调与 Codex Responses 事件分层，将 Grok 的计划型中间消息改为独立 `commentary` 消息并按上游 delta 渐进输出；Agent Loop 仍在后台继续工具恢复，不再等完整首轮与恢复轮全部结束后一次性显示。
- 上游 Chat Completions SSE 改为边读边解析，同时保留完整响应用于工具 JSON 和终态判定；检测到工具调用语法后停止转发该段，避免把内部工具 JSON 混入可见进度。
- 流式读取与聚合拆分到独立 `electron/protocol/chatAssistantStream.js`，显示回调异常或下游断开不会破坏有限恢复状态。
- 脱敏诊断新增首个可见进度耗时和进度 delta 数量，便于区分“上游尚未返回”与“代理未及时转发”。

### 测试

- 新增严格时序回归：上游首个计划 delta 到达但响应尚未结束时，Codex 已能收到带 `phase=commentary` 的可见文本；释放上游后仍能恢复为可执行工具调用。
- 新增 Chat SSE 多分片聚合、逐分片回调、usage 保留和显示回调异常隔离测试。

## 1.2.59 - 2026-08-03

### 修复

- Grok 在工具结果后只说“改用下载工具”“下一步继续执行”等计划时，严格工具恢复现在优先于“补完成标志”，不再把未执行计划交给宽松终态恢复。
- 严格恢复要求上游只返回工具 JSON，并为实时查询和图像生成保留嵌套工具提示；不支持结构化输出的上游仍会自动降级。
- 慢模型的单次恢复预算由 10 秒调整到 15 秒，总预算由 25 秒调整到 40 秒；仍然最多恢复三次，继续保留防死循环限制。

### 测试

- 增加“计划承诺与缺少完成标志同时命中”时的分支优先级回归，验证严格 JSON 模式、512 token 限制和最多三次恢复。
- 固定恢复超时常量断言，并继续验证恢复过程只展示进度、不暴露内部结束标志，最终产生可由 Codex 执行的工具调用。

## 1.2.58 - 2026-08-03

### 修复

- 在线更新现在把安装目录明确绑定到当前客户端 EXE 所在目录，不再误装到 `data/updates` 旁边的新文件夹。
- 静默更新使用 NSIS 的更新模式等待旧进程退出、保留 `data`，安装完成后自动启动同目录的新版本。
- 更新日志记录安装目标类型和自动重启状态，方便区分“安装器已启动”和“新版本已替换”。

### 测试

- 增加安装参数顺序、安装目录安全校验和更新模式参数回归。
- 安装器回归新增真实同目录自更新：确认旧程序文件被替换、`data` 标记保留，并从同一路径启动新版本进程。

## 1.2.57 - 2026-08-03

### 修复

- Projects 同步会把从历史任务识别到的 Local Project 加入 Codex 左侧固定项目列表，同时保留用户原有固定项，避免项目已经存在但左侧只显示合并任务列表。
- Grok 在工具结果后回复“我接下来会……”“下一步我将……”等泛化承诺时继续执行有限恢复，不再因一次恢复超时就把未执行计划误判为任务终态。

### 测试

- 增加空固定项目列表修复、原有固定项保留和同步幂等回归。
- 增加中英文泛化未来承诺识别，以及三次恢复后继续生成 Codex 工具调用的 wire 回归。

## 1.2.56 - 2026-08-03

### 修复

- 从历史任务的 `cwd` 自动创建 Codex 桌面端 Local Project，并写入任务与项目的归属关系；同步时会先关闭 Codex、备份桌面状态、合并必要字段并验证后再启动。
- NewAPI 登录地址默认填入 `https://ainiubi.org`，仍可由用户改成其他兼容平台。
- Grok 的“继续/接着”短句会自动绑定上一条真实任务与最近执行状态，避免用户再次描述同一任务。
- Grok 工具调用解析兼容代码块、嵌套 `tool_call`、`tool`/`tool_name`、`args` 和顶层 `input` 等常见返回格式。
- 工具完成后的结束标志只做一次快速恢复；上游超时但已有完整结果时保留原生结果，不再显示内部“未能完成剩余步骤”提示。
- 三次恢复继续受 25 秒总预算约束，最后一次请求不再越过总超时造成额外等待。

### 测试

- 增加 Local Project 状态合并、备份、幂等、损坏文件保护和启动前失败中止测试。
- 增加 Grok 短句续接、旧提示清理、宽松工具信封解析和结束标志降级测试。

## 1.2.55 - 2026-08-03

### 发布与更新

- 绑定公开 GitHub 仓库作为唯一在线更新源，安装版可在启动时或通过“检查更新”按钮读取正式 Release。
- 增加公开源码、CI、Release 安装包与维护文档的自动发布流程；仓库和发布资产不包含渠道凭据、登录态、日志或本机数据。
- 保留 1.2.54 的 Grok 长工具链上下文连续性修复，并将不应展示的内部 Agent Loop 控制标记从用户可见内容与后续上游上下文中移除。

## 1.2.54 - 2026-08-03

### 修复

- Grok 停顿恢复不再只截取最后 8 条工具消息；恢复上下文会同时保留最近普通对话、原始用户任务、压缩摘要和工具链尾部。
- 内部 Agent Loop 完成/安全停止控制标记不再显示在 Codex 对话中，也不会在下一轮重新发送给上游污染上下文。
- 有限恢复达到次数或时间上限时显示普通中文重试提示，详细停止原因只保留在脱敏诊断日志。

### 诊断

- Grok prompt-emulated 诊断增加源消息数、转发消息数、可执行用户消息数、移除控制标记数和恢复上下文消息数。

## 1.2.53 - 2026-08-03

### 修复

- 会话扫描遇到单个被占用、权限变化或瞬时消失的 JSONL 文件时，跳过该文件并继续返回其他历史任务，不再让整个状态查询失败。
- Grok 工具意图恢复增加单次 10 秒和总计 25 秒预算，达到预算后返回明确安全停止信号，避免上游多次慢响应让界面长期卡住。
- Grok 图像请求恢复提示明确要求通过 Codex 的 `exec` 调用 `tools.image_gen__imagegen`，不能只停在“读取图像技能”。
- 在线更新只有在安装程序确实启动后才退出主程序；启动失败会保留当前程序并显示中文错误。

### 性能与诊断

- Next.js 指纹静态资源启用长期不可变缓存，HTML 继续使用重新验证策略，减少重复启动时的磁盘读取。
- 启动日志拆分静态服务、`loadURL` 和窗口等待耗时，便于定位冷启动瓶颈。
- 工具恢复改用较小输出预算和结构化 JSON 响应，减少无效自然语言重试。

### 新增

- 新增 GitHub Release 在线检查、后台下载、大小限制、SHA-256 校验和重启安装流程。
- 新增公开 CI、发布工作流、维护交接、安全说明和贡献指南。

## 1.2.52

- 用户可见错误统一转换为可操作的中文，同时保留脱敏技术日志。
- 会话、项目和程序位置默认使用易读名称，物理路径按需查看。
- 优化中文字体、字号、行距、卡片密度和删除确认提示。

## 1.2.51

- 合并会话/项目导入入口。
- 新增会话 JSONL 和项目 ZIP 导出，包含路径、安全边界与回滚校验。

## 1.2.50

- 修复删除会话时单文件占用导致整批失败。
- 在线渠道登录简化为“登录并同步”。
- Skill 与 Agent 导入增加结构和字段校验。

## 1.2.49

- Grok 流式工具模式提前发送 Responses 进度事件与心跳。
- 实时信息请求要求通过可用工具获取，最多三次有限恢复后安全停止。

## 1.2.45 - 1.2.48

- 增加多任务 Agent Loop 连续执行、可见进度与精确完成信号。
- 规范跨模型工具项 ID，修复 `fc_`/`ctc_` 切换错误。
- 发布包默认纯净，不携带渠道、登录、缓存或历史日志。
- 增加 Codex 配置和启动进度。

## 1.2.39 - 1.2.44

- 基于轻量代理边界实现 Grok Chat Completions 到 Codex Responses Agent Loop 的适配。
- 动态同步 Key 实际可见且通过完整兼容性门禁的模型。
- 修复 GPT Responses 终态解析、工具结果续答和跨模型上下文。
- 管理器数据统一放入客户端目录，打包体积与启动路径持续优化。

## 1.2.18 - 1.2.32

- 建立稳定的 Provider/API Key/模型切换、会话与 Projects 保留逻辑。
- 增加事务备份、登录回滚、随机本地代理端口、日志脱敏和模块化边界。
- 删除不再使用的 CLI 运行时与冗余打包依赖，保留 Codex 原生工具执行路径。
