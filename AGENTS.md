# 项目协作记录规则

以下规则适用于本项目中的所有 Codex 对话：

1. 每次处理用户消息时，都要把本轮的用户目标、关键判断、实际修改和验证结果追加到项目根目录的 `GROK-OAUTH-REVIEW.md`。
2. 每次最终回复必须明确告诉用户：本轮内容已经写入 `GROK-OAUTH-REVIEW.md`。
3. 记录应让其他电脑、其他 Codex 对话无需读取当前聊天记录也能继续工作。
4. 不得把账号密码、OAuth Token、API Key、完整 `auth.json`、Cookie、验证码、恢复码或其他有效凭据写入记录、源码、测试、日志或聊天回复。
5. 如果用户在消息中提供了敏感值，记录中只能写“用户提供了本地凭据文件”等脱敏事实，不得复制原值。

## 高效开发与发布顺序

1. 常规源码修改前先检查 Git 工作区并保护已有改动；不再为每次修改复制整个源码目录。只有涉及真实用户数据迁移、安装/卸载、破坏性操作或 Git 无法覆盖的文件时，才创建隔离备份并校验。
2. 用户可见变更分两个阶段交付：先完成源码门禁并推送 GitHub，明确告知可拉取的提交；再构建、验证并发布 GitHub Release 安装包。
3. 源码阶段至少执行格式、lint、类型检查、相关回归测试、完整模块/核心/协议测试和生产构建。安装包阶段执行纯净目录、打包 UI、NSIS、安装/启动/卸载回归和发布资产校验。
4. 如果改动触及安装器、更新器、Electron 生命周期、持久化路径、数据迁移或发布脚本，相关安装包验证必须前移到源码推送前；不得以分阶段为由跳过适用测试。
5. 用户需要共享目录交付时，只维护一个不带版本号的稳定程序文件夹，不再创建逐版本目录或复制安装包。首次创建后，后续更新必须先确认程序已退出，并备份、保留和验证其中的 `data` 用户数据；不得直接删除或覆盖正在使用的数据目录。
6. “源码热更新”仅指开发工作区拉取 GitHub 提交后使用开发模式热重载；已安装客户端仍通过 GitHub Release 安装包更新，不能把仅推送源码描述为客户端已经可在线更新。
