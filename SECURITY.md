# 安全策略

## 报告安全问题

请通过 GitHub 的私密安全报告功能提交漏洞，不要在公开 Issue 中粘贴以下内容：

- GitHub PAT、API Key、Authorization 或 Cookie；
- `auth.json`、渠道状态文件或浏览器登录数据；
- 未脱敏的日志、会话正文、工具结果或本机完整路径。

如果凭据已经出现在聊天、Issue、提交或日志中，应立即在对应服务撤销并重新生成。删除公开文本不能代替撤销。

## 支持范围

安全修复只针对当前最新发布版本。项目不会要求用户把真实凭据提交到仓库，也不会在 Release 中附带开发机数据。

## 更新信任边界

客户端只接受当前项目 GitHub Release 中名称精确匹配的 Windows x64 安装包，并验证 GitHub 提供的 SHA-256 摘要、文件大小和下载完整性。当前安装包未使用商业代码签名证书。
